<%= form_for(@researcharea) do |f| %>
  <% if @researcharea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@researcharea.errors.count, "error") %> prohibited this researcharea from being saved:</h2>

      <ul>
      <% @researcharea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
