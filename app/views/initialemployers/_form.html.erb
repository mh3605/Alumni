<%= form_for(@initialemployer) do |f| %>
  <% if @initialemployer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@initialemployer.errors.count, "error") %> prohibited this initialemployer from being saved:</h2>

      <ul>
      <% @initialemployer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
